"""Infrastructure behavior library for big game systems.

This library provides behavior modules for:
- Scheduled events (turn_phase_scheduled)
- Commitment tracking (turn_phase_commitment)
- Gossip propagation (turn_phase_gossip)
- Environmental spreads (turn_phase_spread)

Each module registers its turn phase handler via vocabulary.
"""
