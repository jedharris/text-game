# Damaged Guardian Item Use Walkthrough
# Tests: Item_use reactions, state transitions, repair sequences
# STATUS: Blocked — @set inventory doesn't register items for use/give,
# stone_chisel can't be taken (vocabulary issue?), state_machine has no .current field.

# Stabilize Aldric
@set actors.npc_aldric.properties.health = 100

# Go to guardian location
@goto broken_statue_hall

# Test 1: Examine guardian in non-functional state
{"type":"command","action":{"verb":"examine","object":"guardian"}}
@expect "guardian"

# Test 2: Try to talk (should get minimal response)
{"type":"command","action":{"verb":"talk","indirect_object":"guardian"}}
@expect "still and silent"

# Test 3: Use animator_crystal to awaken guardian
# GAME BUG: @set inventory doesn't register items for use command
@set actors.player.inventory = ["animator_crystal"]
{"type":"command","action":{"verb":"use","object":"animator_crystal","indirect_object":"guardian"}} # EXPECT_FAIL

# Test 4: Try to take stone_chisel (at this location)
# GAME BUG: stone_chisel can't be taken despite being at broken_statue_hall
{"type":"command","action":{"verb":"take","object":"stone chisel"}} # EXPECT_FAIL

# Test 5: Dialog with guardian (still non-functional since repair couldn't proceed)
{"type":"command","action":{"verb":"talk","indirect_object":"guardian"}}
@expect "guardian"

# Summary: Guardian examine and talk work, but item_use progression blocked
# GAME BUGS:
# - @set actors.player.inventory doesn't register items for use command
# - stone_chisel can't be taken despite correct location
# - state_machine has no .current field to assert on
